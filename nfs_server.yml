---
-name: this is my first playbook
 hosts: all
 tasks:
  -name: installing httpd packages
   yum: 
      name: httpd
      state: installed

-name: starting the service 
 service:
    name: httpd
    state: started
  
